<nav class="navbar navbar-light navbar-expand-lg" aria-label="">
  <div class="container-xl">
    <%= link_to "Serhii's blog", root_path, class:"navbar-brand", id:"logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample07XL" aria-controls="navbarsExample07XL" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExample07XL">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Bloggers", users_path, class:"nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Articles</a>
          <ul class="dropdown-menu">
            <% if logged_in? %>
              <li><%= link_to "Create a new article", new_article_path, class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to "View articles", articles_path, class: "dropdown-item" %></li>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Categories</a>
          <ul class="dropdown-menu">
            <% if logged_in? && current_user.admin? %>
              <li><%= link_to "Create a new category", new_category_path, class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to "View categories", categories_path, class: "dropdown-item" %></li>
          </ul>
        </li>
        <li class="nav-item">
          <%= link_to "About me", about_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Contact</a>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
              <%= '(Admin)' if current_user.admin? %> Profile [<%= current_user.username %>]
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "View your profile", user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to "Edit your profile", edit_user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to "Delete your profile", user_path(current_user), class: "dropdown-item text-danger", method: :delete, data: { confirm: "Are you sure?" } %></li>
              <li><%= link_to "Create a new article", new_article_path, class: "dropdown-item" %></li>
            </ul>
          </li>
          <li>
            <%= link_to "Logout", logout_path, class:"nav-link", method: :delete, data: {turbo_method: :delete} %>
          </li>
        <% else %>
          <li>
            <%= link_to "Login", login_path, class:"nav-link" %>
          </li>
          <li>
            <%= link_to "Sign up", signup_path, class: "nav-link text-capitalize" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
